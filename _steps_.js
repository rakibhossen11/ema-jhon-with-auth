/**
 * 1. create firebase project
 * 2. create web app 
 * 3. npm i firebase
 * 4. save firebase config and export default app
 * 5. build > authentication > enable sign in method
 * 6. create sign up and log in route
 * */ 

/**
 * 1. create a context provider file
 * 2. create a context and set provider
 * 3. set children propes
 * 4. set context value
 * 5. set the provider
 * */ 


/**
 * HOSTING
 *  One time per PC 
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * --------------------
 * for each project one time
 * 1. firebase init 
 * 2. procced
 * 3. hosting: firebase(up and down arrow) use space bar to select
 * 4. existing project 
 * 5. select the project carefully
 * 6. which project as public directory:  (is vit: dist),(is react: build)
 * 7. single page application: yes
 * 8. continuous deploument : no
 * 
 * For every time deploy
 * 1. npm run build
 * 2. firebase deploy
 * 3. 
 * 
 * */ 